<% if (blogs.length === 0) { %>
        <p>No blogs found.</p>
      <% } else { %>
        <% blogs.forEach(blog => { %>
          <%- include('partials/blogCard', { blog }) %>
        <% }); %>
      <% } %>
      